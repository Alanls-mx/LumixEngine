# Assets da Aplicação

Coloque nesta pasta assets locais que serão importados via TypeScript/React (como imagens, SVGs de componentes, vetores ou ilustrações).

Para assets estáticos servidos diretamente na raiz (como `favicon.svg`, `robots.txt`), utilize o diretório `/public`.
